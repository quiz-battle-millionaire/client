export const SET_QUESTIONS = (state, question) => {
    state.question = question
  }
  
export const SET_PLAYERS = (state, users) => {
    state.player=users
  } 
  export const SET_WINNER = (state, winner) => {
    state.winner=winner
  } 